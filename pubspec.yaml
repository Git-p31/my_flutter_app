name: my_flutter_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter_inappwebview: ^5.7.2+3  # Повернуто на старішу версію для сумісності
  youtube_player_flutter: ^8.1.2   # Сумісна версія YouTube Player
  http: ^0.13.6                    # Оновлена версія HTTP
  cupertino_icons: ^1.0.8           # Остання версія Cupertino Icons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
